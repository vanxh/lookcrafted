{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"outputDirectory": "dist",
	"buildCommand": "bun build api/index.tsx --outdir dist --target bun",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.js"
		}
	]
}

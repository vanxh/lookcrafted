{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/api"
		}
	],
	"outputDirectory": "dist",
	"buildCommand": "bun build api --outdir=dist --target=bun"
}
